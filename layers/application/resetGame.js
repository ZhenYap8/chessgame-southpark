import { createInitialPosition } from '../domain/Board.js';

// resetGame (stub) - will use domain initial board
// TODO replace with actual position factory in step 3
export function resetGame(){
  return createInitialPosition();
}
